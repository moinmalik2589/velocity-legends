# Testing

## Baseline

- `npm run build` succeeds.
- Vite development server is configured for port 1807 with strict port enabled.

## Required coverage

Unit and integration tests will cover profile persistence, purchases, upgrades, rewards, checkpoints, laps, position, countdown, pause, and finish idempotency. Android and backend tests require their respective projects in later milestones.
